# Apps Installer

Графический установщик для загрузки популярного программного обеспечения.

## 📋 Описание

Программа "Apps installer" представляет собой графический интерфейс на базе библиотеки Tkinter для выбора и последующей загрузки установочных файлов различного программного обеспечения. Программа автоматически определяет операционную систему и загружает соответствующие версии приложений.

## ✨ Основные функции

- **Выбор программ**: Пользователь может выбрать одну или несколько программ из предложенного списка категорий
- **Автоматическое определение ОС**: Программа автоматически определяет вашу операционную систему (Windows/macOS) и показывает только доступные приложения
- **Загрузка файлов**: После выбора программ и указания папки для сохранения, программа загружает установочные файлы программ в фоновом режиме
- **Отображение статуса**: Показывает текущий статус загрузки и прогресс для каждого файла
- **Динамические ссылки**: Использует latest версии и обрабатывает редиректы для актуальных ссылок
- **Информация о программе**: Позволяет просмотреть информацию о создателе и список всех ссылок на скачивание

## 🚀 Быстрый старт

### Запуск из исходников

1. Убедитесь, что у вас установлен Python 3.7 или выше
2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
3. Запустите программу:
   ```bash
   python main.py
   ```

## 🔨 Сборка исполняемых файлов

### Windows

1. Убедитесь, что у вас установлен Python 3.7+
2. Запустите скрипт сборки:
   ```bash
   python build_windows.py
   ```
3. Исполняемый файл будет находиться в папке `dist/Apps installer.exe`

### macOS

1. Убедитесь, что у вас установлен Python 3.7+
2. Запустите скрипт сборки:
   ```bash
   python3 build_macos.py
   ```
   Или используйте bash скрипт:
   ```bash
   chmod +x build_macos.sh
   ./build_macos.sh
   ```
3. Исполняемый файл будет находиться в папке `dist/Apps installer`

### Требования для сборки

- Python 3.7 или выше
- PyInstaller (устанавливается автоматически скриптами сборки)
- Все зависимости из `requirements.txt`

## 📁 Структура проекта

```
Apps_installer/
├── main.py              # Основной файл приложения
├── config.json          # Конфигурационный файл со ссылками и категориями
├── requirements.txt     # Зависимости Python
├── build_windows.py     # Скрипт сборки для Windows
├── build_macos.py       # Скрипт сборки для macOS
├── build_macos.sh       # Альтернативный скрипт сборки для macOS
├── .gitignore           # Исключения для git
└── README.md            # Этот файл
```

## ⚙️ Конфигурация

Все ссылки на загрузку и категории программ хранятся в файле `config.json`. Это позволяет легко обновлять ссылки без изменения кода.

### Обновление ссылок

Откройте `config.json` и обновите URL для нужных приложений. Программа автоматически обработает редиректы и загрузит актуальные версии.

### Добавление новых программ

1. Откройте `config.json`
2. Добавьте новую программу в секцию `downloads`:
   ```json
   "Название программы": {
     "Windows": {
       "url": "ссылка_на_загрузку.exe",
       "filename": "имя_файла.exe"
     },
     "Darwin": {
       "url": "ссылка_на_загрузку.dmg",
       "filename": "имя_файла.dmg"
     }
   }
   ```
3. Добавьте программу в соответствующую категорию в секции `categories`

## 📝 Использование

1. **Выбор программ**: Отметьте чекбоксы для программ, которые хотите загрузить
2. **Выбор папки для сохранения**: Нажмите кнопку "Загрузить" и выберите папку для сохранения файлов
3. **Мониторинг загрузки**: Следите за прогрессом загрузки и текущим статусом в окне программы
4. **Просмотр информации**: Нажмите кнопку "Информация", чтобы увидеть контактные данные создателя и список всех доступных для загрузки программ

## 🔧 Технические детали

- **Платформа**: Кроссплатформенное (Windows, macOS)
- **GUI Framework**: Tkinter
- **Язык**: Python 3.7+
- **Зависимости**: 
  - `requests` - для загрузки файлов
  - `PyInstaller` - для сборки исполняемых файлов (только при сборке)

## 📦 Зависимости

```
requests>=2.31.0
```

## ⚠️ Важные замечания

1. Программа автоматически определяет вашу операционную систему и показывает только доступные для неё приложения
2. Ссылки обновляются автоматически через обработку редиректов
3. Исполняемые файлы (.exe, .dmg) исключены из git репозитория через `.gitignore`
4. Для обновления ссылок на новые версии программ редактируйте `config.json`

## 📄 Лицензия

Ответственности создатель не несет, используйте на свой риск.

## 👤 Автор

**offach**
- Контакт: me@offach.ru

## 🔄 Обновления

Программа использует latest версии ссылок и автоматически обрабатывает редиректы, что позволяет ей работать актуально даже через длительное время после создания.

---

**Примечание**: Для корректной работы программы необходим файл `config.json` в той же папке, что и исполняемый файл.
